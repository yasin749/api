module.exports = function (database) {
    database.beforeConnect(async (config) => {

    });

    database.afterConnect(async (config) => {

    });

    database.beforeDisconnect(async (config) => {

    });

    database.afterDisconnect(async (config) => {

    });
}



